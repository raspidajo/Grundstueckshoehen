Sandbox: simple mesh plotting

Run the example to generate `sandbox_mesh_example.png`:

```bash
python -c "from sandbox.simple_mesh import main; main(show=False)"
```

To open interactively (will show Matplotlib window):

```bash
python -c "from sandbox.simple_mesh import main; main(show=True)"
```
