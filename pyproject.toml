[project]
name = "grundstueckshoehen"
version = "0.1.0"
description = "Gelände- und Hauspositionen visualisieren, Mesh erzeugen und Steigung farbig darstellen."
readme = "README.md"
requires-python = ">=3.10"
authors = [ { name = "Johannes" } ]

[project.dependencies]
matplotlib = "^3.8"
numpy = "^1.27"
scipy = "^1.12"

[project.scripts]
groundplot = "grundstueckshoehen.__main__:main"
